#!/bin/sh

set -eu

export SSH_ASKPASS="$(which ssh_askpass_1pw.sh)"
export SSH_ASKPASS_REQUIRE=force

if [ ! -x "$SSH_ASKPASS" ]; then
    echo "Not executable $SSH_ASKPASS"
fi

export OP_ITEM_ID=$1
shift

exec ssh-add -t 12h "$@"
